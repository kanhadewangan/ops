apiVersion: v1
kind: Service
metadata:
  name: my-app
spec:
  selector:
    matchLabels:
      app: my-app
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
  template:
    metadata:
      labels:
        app: my-app
    spec:
      ports:
      - containerPort: 80